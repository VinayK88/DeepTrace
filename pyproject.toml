[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deeptrace"
version = "0.2.0"
description = "Content authenticity, provenance, and ML-assisted influence-campaign analysis workbench"
requires-python = ">=3.10"
dependencies = ["scikit-learn==1.7.2"]

[project.optional-dependencies]
api = ["fastapi>=0.110", "uvicorn>=0.29"]

[project.scripts]
deeptrace = "deeptrace.cli:main"

[tool.setuptools.packages.find]
include = ["deeptrace*"]
